version: '3.8'

services:
  db:
    image: postgres:13.10
    restart: always
    environment:
      POSTGRES_DB: wiki
      POSTGRES_USER: Free
      POSTGRES_PASSWORD: Willy
      PGPASSWORD: Willy
    ports:
    - "5432:5432" 

